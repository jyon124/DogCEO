describe('DogApi.fetchRandomDogImgs', () => {
    it('calls the fetch API and return the JSON data');
});

describe('DogApi.fetchDogBreeds', () => {
    it('calls the fetch API and return the JSON data');
});